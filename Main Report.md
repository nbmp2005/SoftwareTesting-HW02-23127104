# Thông tin chung
- Họ và tên: Nguyễn Bình Minh Phương
- MSSV: 23127104

# FR-03: Quên mật khẩu & Đặt lại mật khẩu (2 bước)

## I. DOMAIN TESTING

### 1. DETAILED STEP-BY-STEP DOMAIN ANALYSIS

Tính năng FR-03 gồm hai bước rõ ràng. Ở Bước 1, người dùng chỉ cần cung cấp email đã đăng ký để hệ thống sinh OTP 6 chữ số và gắn OTP đó với đúng email yêu cầu. Ở Bước 2, người dùng nhập OTP, mật khẩu mới và xác nhận mật khẩu mới để hoàn tất đặt lại mật khẩu. Vì đây là luồng quên mật khẩu nên các input cần được phân tích theo logic nghiệp vụ thay vì chỉ nhìn như một form nhập liệu thông thường.

Khi phân tích miền tương đương, email là điều kiện khởi tạo luồng reset. Email phải đúng định dạng và phải tồn tại trong hệ thống, nên miền hợp lệ của email là một miền duy nhất có hai thuộc tính đồng thời: hợp lệ về format và hợp lệ về dữ liệu tra cứu. OTP là đầu vào xác thực quan trọng ở Bước 2; nó phải đúng 6 chữ số và phải thuộc đúng email đã yêu cầu. Vì vậy OTP không chỉ có miền hợp lệ/không hợp lệ theo định dạng, mà còn có miền hợp lệ/không hợp lệ theo quan hệ với email. Mật khẩu mới là input chịu policy bảo mật, nên miền hợp lệ của nó chỉ tồn tại khi đồng thời thỏa toàn bộ điều kiện: tối thiểu 8 ký tự, có chữ hoa, chữ thường, chữ số và ký tự đặc biệt hợp lệ. Xác nhận mật khẩu mới là input phụ thuộc hoàn toàn vào mật khẩu mới và chỉ có hai miền logic: trùng hoặc không trùng.

Từ góc nhìn kiểm thử miền, các output như thông báo lỗi, thông báo thành công, step indicator hay nút quay lại đăng nhập không tạo thành miền dữ liệu riêng để phân lớp tương đương. Chúng là expected result của từng miền input. Điểm quan trọng cần giữ là OTP chỉ có hiệu lực cho email đã yêu cầu; nếu cùng một OTP bị nhập cho email khác thì hệ thống phải từ chối. Điều này khiến OTP trở thành một input có ràng buộc chéo với email, không thể kiểm thử tách rời hoàn toàn.

### 2. DOMAIN TEST CASES TABLE

| Test Case ID | Description | Pre-condition | Input Data | Test Steps | Expected Result | Actual Result | Status | Defect ID | Tested By | Date Tested |
|---|---|---|---|---|---|---|---|---|---|---|
| TC-01 | Reset mật khẩu thành công với dữ liệu hợp lệ | Người dùng đã có email đăng ký hợp lệ; đang ở màn hình quên mật khẩu | Email: `test@eshop.com`;<br>OTP: Lấy từ hiển thị demo;<br>Mật khẩu mới: `Passw0rd@1`;<br>Xác nhận: `Passw0rd@1` | 1. Nhập email `test@eshop.com` ở Bước 1<br>2. Nhấn lấy OTP<br>3. Quan sát OTP hiển thị trên demo và ghi lại giá trị<br>4. Nhập OTP ở Bước 2<br>5. Nhập mật khẩu mới `Passw0rd@1`<br>7. Gửi form | Hệ thống chấp nhận yêu cầu reset mật khẩu và hiển thị thành công | Hệ thống vẫn báo lỗi "Mật khẩu quá yếu! Phải dài tối thiểu 8 ký tự, gồm chữ hoa, chữ thường, số và KÝ TỰ ĐẶC BIỆT." dù mật khẩu đúng yêu cầu | Failed | BUG-FR03-TC01 | Minh Phương | 08/07/2026 |
| TC-02 | Kiểm tra email hợp lệ và tồn tại khi lấy OTP | Người dùng đang ở Bước 1 | Email: `test@eshop.com`; OTP: Lấy từ hiển thị demo | 1. Nhập email `test@eshop.com`<br>2. Nhấn lấy OTP<br>3. Ghi nhận OTP hiển thị hiển thị demo | Hệ thống sinh OTP 6 chữ số và hiển thị/gửi OTP theo môi trường demo | Hệ thống sinh OTP 4 chữ số và hiển thị theo môi trường demo | Failed | BUG-FR03-TC02 | Minh Phương | 08/07/2026 |
| TC-03 | Email sai định dạng | Người dùng đang ở Bước 1 | Email: `test@eshop` | 1. Nhập email `test@eshop`<br>2. Nhấn lấy OTP | Hệ thống từ chối và hiển thị thông báo lỗi email không hợp lệ | Hệ thống từ chối và hiển thị thông báo lỗi "User not found"| Failed | BUG-FR03-TC03 | Minh Phương | 08/07/2026 |
| TC-04 | Email hợp lệ nhưng không tồn tại | Người dùng đang ở Bước 1 | Email: `khongtontai@gmail.com` | 1. Nhập email `khongtontai@gmail.com`<br>2. Nhấn lấy OTP | Hệ thống từ chối và hiển thị người dùng không tồn tại | Hệ thống từ chối và hiển thị thông báo lỗi "User not found" | Passed |  | Minh Phương | 08/07/2026 |
| TC-05 | OTP đúng độ dài (6 ký tự) nhưng chứa ký tự không phải số | Người dùng đã lấy OTP ở Bước 1 | Email: `test@eshop.com`;<br>OTP: Lấy từ hiển thị demo | 1. Nhập email `test@eshop.com`<br>2. Sang Bước 2<br>3. Nhập OTP `48291A` | Hệ thống từ chối và báo OTP không hợp lệ | Hệ thống báo lỗi "Mật khẩu quá yếu! Phải dài tối thiểu 8 ký tự, gồm chữ hoa, chữ thường, số và KÝ TỰ ĐẶC BIỆT." | Failed | BUG-FR03-TC04 | Minh Phương | 08/07/2026 |
| TC-06 | OTP đúng định dạng nhưng sai giá trị | Người dùng đã lấy OTP ở Bước 1 | Email: `test@eshop.com`;<br>OTP: `123456` | 1. Nhập email `test@eshop.com`<br>2. Sang Bước 2<br>3. Nhập OTP `123456` | Hệ thống từ chối và báo OTP không hợp lệ hoặc không khớp |  Hệ thống báo lỗi "Mật khẩu quá yếu! Phải dài tối thiểu 8 ký tự, gồm chữ hoa, chữ thường, số và KÝ TỰ ĐẶC BIỆT." | Failed | BUG-FR03-TC05 | Minh Phương | 08/07/2026 |
| TC-07 | OTP hợp lệ nhưng thuộc email khác | Có ít nhất 2 email đã đăng ký | Email A: `test@eshop.com`;<br>Email B: `admin@eshop.com`;<br>OTP: Hiển thị ừ demo (lấy từ email A) | 1. Lấy OTP cho email A `test@eshop.com`<br>2. Sang Bước 2 của email B `admin@eshop.com`<br>3. Nhập OTP của email A | Hệ thống từ chối vì OTP không khớp với email đang yêu cầu | Hệ thống báo lỗi "Mật khẩu quá yếu! Phải dài tối thiểu 8 ký tự, gồm chữ hoa, chữ thường, số và KÝ TỰ ĐẶC BIỆT." | Failed | BUG-FR03-TC06 | Minh Phương | 08/07/2026 |
| TC-08 | Mật khẩu mới ngắn hơn 8 ký tự | Người dùng đã qua OTP hợp lệ | Email: `test@eshop.com`;<br>OTP: hiển thị trên demo ;<br>Mật khẩu mới: `Pw0rd@1`;<br>Xác nhận: `Pw0rd@1` | 1. Nhập OTP<br>2. Nhập mật khẩu mới `Pw0rd@1`<br>3. Nhập xác nhận `Pw0rd@1`<br>4. Gửi form | Hệ thống từ chối và báo mật khẩu không đạt policy | Hệ thống báo lỗi "Mật khẩu quá yếu! Phải dài tối thiểu 8 ký tự, gồm chữ hoa, chữ thường, số và KÝ TỰ ĐẶC BIỆT." | Passed |  | Minh Phương | 08/07/2026 |
| TC-09 | Mật khẩu mới thiếu ký tự đặc biệt | Người dùng đã qua OTP hợp lệ | Email: `test@eshop.com`;<br>OTP: hiển thị trên demo;<br>Mật khẩu mới: `Passw0rd1`;<br>Xác nhận: `Passw0rd1` | 1. Nhập OTP <br>2. Nhập mật khẩu mới `Passw0rd1`<br>3. Nhập xác nhận `Passw0rd1`<br>4. Gửi form | Hệ thống từ chối và báo mật khẩu không đạt policy | Hệ thống báo lỗi "Mật khẩu quá yếu! Phải dài tối thiểu 8 ký tự, gồm chữ hoa, chữ thường, số và KÝ TỰ ĐẶC BIỆT." | Passed |  | Minh Phương | 08/07/2026 |
| TC-10 | Xác nhận mật khẩu không khớp | Người dùng đã qua OTP hợp lệ | Email: `test@eshop.com`;<br>OTP: hiển thị demo ;<br>Mật khẩu mới: `Passw0rd@1`;<br>Xác nhận: `Passw0rd@2` | 1. Nhập OTP hiển thị demo<br>2. Nhập mật khẩu mới `Passw0rd@1`<br>3. Nhập xác nhận `Passw0rd@2`<br>4. Gửi form | Hệ thống từ chối và báo hai mật khẩu không khớp | Trên giao diện Web hiện tại không có trường "Xác nhận mật khẩu mới". Không thể thực hiện tiếp bước 3. | Blocked | BUG-FR03-TC07 | Minh Phương | 08/07/2026 |
| TC-11 | Mật khẩu mới thiếu chữ hoa | Người dùng đã qua OTP hợp lệ | Email: `test@eshop.com`; OTP: hiển thị demo; Mật khẩu mới: `passw0rd@1`; Xác nhận: `passw0rd@1` | 1. Nhập OTP hiển thị demo<br>2. Nhập mật khẩu mới `passw0rd@1`<br>3. Nhập xác nhận `passw0rd@1`<br>4. Gửi form | Hệ thống từ chối và báo mật khẩu không đạt policy (thiếu chữ hoa) | Hệ thống từ chối và báo mật khẩu không đạt policy | Hệ thống báo lỗi "Mật khẩu quá yếu! Phải dài tối thiểu 8 ký tự, gồm chữ hoa, chữ thường, số và KÝ TỰ ĐẶC BIỆT." | Failed | BUG-FR03-TC08 | Minh Phương | 08/07/2026 |
| TC-12 | Mật khẩu mới thiếu chữ thường | Người dùng đã qua OTP hợp lệ | Email: `test@eshop.com`; OTP: hiển thị demo; Mật khẩu mới: `PASSW0RD@1`; Xác nhận: `PASSW0RD@1` | 1. Nhập OTP hiển thị demo<br>2. Nhập mật khẩu mới `PASSW0RD@1`<br>3. Nhập xác nhận `PASSW0RD@1`<br>4. Gửi form | Hệ thống từ chối và báo mật khẩu không đạt policy (thiếu chữ thường) | Hệ thống báo lỗi "Mật khẩu quá yếu! Phải dài tối thiểu 8 ký tự, gồm chữ hoa, chữ thường, số và KÝ TỰ ĐẶC BIỆT." | Failed | BUG-FR03-TC09 | Minh Phương | 08/07/2026 |
| TC-13 |Mật khẩu mới thiếu chữ số | Người dùng đã qua OTP hợp lệ | Email: `test@eshop.com`; OTP: hiển thị demo; Mật khẩu mới: `Password@`; Xác nhận: `Password@` | 1. Nhập OTP hiển thị demo<br>2. Nhập mật khẩu mới `Password@`<br>3. Nhập xác nhận `Password@`<br>4. Gửi form | Hệ thống từ chối và báo mật khẩu không đạt policy (thiếu chữ số) | Hệ thống báo lỗi "Mật khẩu quá yếu! Phải dài tối thiểu 8 ký tự, gồm chữ hoa, chữ thường, số và KÝ TỰ ĐẶC BIỆT." | Failed | BUG-FR03-TC10 | Minh Phương | 08/07/2026 |
| TC-14 | Mật khẩu mới chứa ký tự đặc biệt không nằm trong whitelist | Người dùng đã qua OTP hợp lệ | Email: `test@eshop.com`; OTP: hiển thị demo; Mật khẩu mới: `Passw0rd#1`; Xác nhận: `Passw0rd#1` | 1. Nhập OTP hiển thị demo<br>2. Nhập mật khẩu mới `Passw0rd#1`<br>3. Nhập xác nhận `Passw0rd#1`<br>4. Gửi form | Hệ thống từ chối vì `#` không thuộc danh sách ký tự đặc biệt hợp lệ (`@ $ ! % * ? &`) | Hệ thống báo lỗi "Mật khẩu quá yếu! Phải dài tối thiểu 8 ký tự, gồm chữ hoa, chữ thường, số và KÝ TỰ ĐẶC BIỆT." | Failed | BUG-FR03-TC11 | Minh Phương | 08/07/2026 |
| TC-15 | Email để trống | Người dùng đang ở Bước 1 | Email: `` (rỗng) | 1. Để trống ô Email<br>2. Nhấn lấy OTP | Hệ thống từ chối và báo lỗi bắt buộc nhập email | Hệ thống từ chối và báo lỗi bắt buộc nhập email | Passed |  | Minh Phương | 08/07/2026 |
| TC-16 | OTP để trống | Người dùng đã lấy OTP ở Bước 1 | Email: `test@eshop.com`; OTP: `` (rỗng) | 1. Sang Bước 2<br>2. Để trống ô OTP<br>3. Nhập mật khẩu mới `Passw0rd@1`<br>4. Nhập xác nhận `Passw0rd@1`<br>5. Gửi form | Hệ thống từ chối và báo lỗi bắt buộc nhập OTP | Hệ thống từ chối và báo lỗi bắt buộc nhập OTP | Passed |  | Minh Phương | 08/07/2026 |

## II. BOUNDARY VALUE ANALYSIS (BVA)

### 1. DETAILED STEP-BY-STEP BOUNDARY ANALYSIS

BVA của FR-03 tập trung vào các điểm biên có ý nghĩa thực tế trong đặc tả. Biên rõ ràng nhất là OTP 6 chữ số, vì đây là mã có độ dài cố định và thường phát sinh lỗi ở các trường hợp thiếu số 0 đầu, vượt độ dài hoặc bị nhập sai số lượng ký tự. Biên thứ hai là mật khẩu mới với điều kiện tối thiểu 8 ký tự; tại đây điểm gãy chủ yếu nằm ở 7, 8 và 9 ký tự. Với xác nhận mật khẩu mới, không tồn tại biên số học, nên mình coi đây là biên logic giữa hai trạng thái: khớp tuyệt đối và không khớp tối thiểu. Email không được đưa vào BVA chuẩn vì đặc tả hiện tại không nêu min/max độ dài hay khoảng giá trị số học; email phù hợp hơn với Domain Testing và kiểm thử định dạng.

Chọn single-fault BVA vì nó phản ánh đúng mục tiêu của bài kiểm thử biên: tại mỗi test case chỉ một input bị đẩy ra biên, các input còn lại giữ ở vùng hợp lệ. Với cách này, nếu test fail thì nguyên nhân thường quy được về chính biên đang xét. OTP được xem xét theo cả độ dài và miền 6 chữ số hợp lệ, trong đó các giá trị 000000, 000001, 999998 và 999999 đại diện cho vùng sát biên dưới và sát biên trên. Mật khẩu mới được xét quanh biên tối thiểu 8 ký tự: 7, 8, 9 ký tự. Đây là biên quan trọng nhất vì policy hiện không cung cấp biên trên, do đó không nên tự đặt thêm max nếu chưa được đặc tả. Xác nhận mật khẩu mới được kiểm thử theo mức khớp đúng, lệch 1 ký tự và hoàn toàn khác nhau để đảm bảo hệ thống xử lý đúng trạng thái không khớp.

### 2. BOUNDARY TEST CASES TABLE

| Test Case ID | Description | Pre-condition | Input Data | Test Steps | Expected Result | Actual Result | Status | Defect ID | Tested By | Date Tested |
|---|---|---|---|---|---|---|---|---|---|---|
| TC-BVA-01 | OTP dưới biên độ dài: 5 chữ số | Người dùng đã lấy OTP cho email hợp lệ | Email: `test@eshop.com`;<br>OTP: `12345` | 1. Nhập email `test@eshop.com` ở Bước 1<br>2. Sang Bước 2<br>3. Nhập OTP `12345`<br>4. Nhập mật khẩu mới `Passw0rd@1`<br>5. Nhập xác nhận `Passw0rd@1`<br>6. Gửi form | Hệ thống từ chối vì OTP không đạt biên 6 chữ số |  |  |  |  | Cần người dùng thực thi thủ công hoặc chạy trên SUT rồi điền lại |
| TC-BVA-02 | OTP tại biên dưới: 6 chữ số nhỏ nhất hợp lệ | Có OTP được kiểm soát/seed trong môi trường test | Email: `test@eshop.com`;<br>OTP: `000000` (lấy từ seed/demo) | 1. Nhập email `test@eshop.com` ở Bước 1<br>2. Sang Bước 2<br>3. Nhập OTP `000000`<br>4. Nhập mật khẩu mới `Passw0rd@1`<br>5. Nhập xác nhận `Passw0rd@1`<br>6. Gửi form | Hệ thống chấp nhận OTP nếu nó đúng với email đã yêu cầu và cho phép reset mật khẩu |  |  |  |  | Cần người dùng thực thi thủ công hoặc chạy trên SUT rồi điền lại |
| TC-BVA-03 | OTP ngay trên biên dưới trong miền 6 chữ số | Có OTP được kiểm soát/seed trong môi trường test | Email: `test@eshop.com`;<br>OTP: `000001` (lấy từ seed/demo) | 1. Nhập email `test@eshop.com` ở Bước 1<br>2. Sang Bước 2<br>3. Nhập OTP `000001`<br>4. Nhập mật khẩu mới `Passw0rd@1`<br>5. Nhập xác nhận `Passw0rd@1`<br>6. Gửi form | Hệ thống chấp nhận OTP nếu đúng với email đã yêu cầu |  |  |  |  | Cần người dùng thực thi thủ công hoặc chạy trên SUT rồi điền lại |
| TC-BVA-04 | OTP ngay dưới biên trên trong miền 6 chữ số | Có OTP được kiểm soát/seed trong môi trường test | Email: `test@eshop.com`;<br>OTP: `999998` (lấy từ seed/demo) | 1. Nhập email `test@eshop.com` ở Bước 1<br>2. Sang Bước 2<br>3. Nhập OTP `999998`<br>4. Nhập mật khẩu mới `Passw0rd@1`<br>5. Nhập xác nhận `Passw0rd@1`<br>6. Gửi form | Hệ thống chấp nhận OTP nếu đúng với email đã yêu cầu |  |  |  |  | Cần người dùng thực thi thủ công hoặc chạy trên SUT rồi điền lại |
| TC-BVA-05 | OTP tại biên trên: 6 chữ số lớn nhất hợp lệ | Có OTP được kiểm soát/seed trong môi trường test | Email: `test@eshop.com`;<br>OTP: `999999` (lấy từ seed/demo) | 1. Nhập email `test@eshop.com` ở Bước 1<br>2. Sang Bước 2<br>3. Nhập OTP `999999`<br>4. Nhập mật khẩu mới `Passw0rd@1`<br>5. Nhập xác nhận `Passw0rd@1`<br>6. Gửi form | Hệ thống chấp nhận OTP nếu đúng với email đã yêu cầu |  |  |  |  | Cần người dùng thực thi thủ công hoặc chạy trên SUT rồi điền lại |
| TC-BVA-06 | OTP trên biên độ dài: 7 chữ số | Người dùng đã lấy OTP cho email hợp lệ | Email: `test@eshop.com`;<br>OTP: `1234567` | 1. Nhập email `test@eshop.com` ở Bước 1<br>2. Sang Bước 2<br>3. Nhập OTP `1234567`<br>4. Nhập mật khẩu mới `Passw0rd@1`<br>5. Nhập xác nhận `Passw0rd@1`<br>6. Gửi form | Hệ thống từ chối vì OTP vượt biên độ dài cho phép |  |  |  |  | Cần người dùng thực thi thủ công hoặc chạy trên SUT rồi điền lại |
| TC-BVA-07 | Mật khẩu mới dưới biên: 7 ký tự | Người dùng đã qua OTP hợp lệ | Email: `test@eshop.com`;<br>OTP: hiển thị demo;<br>Mật khẩu mới: `Pw0rd@1`;<br>Xác nhận: `Pw0rd@1` | 1. Nhập OTP hiển thị demo<br>2. Nhập mật khẩu mới `Pw0rd@1`<br>3. Nhập xác nhận `Pw0rd@1`<br>4. Gửi form | Hệ thống từ chối vì mật khẩu dưới độ dài tối thiểu 8 ký tự |  |  |  |  | Cần người dùng thực thi thủ công hoặc chạy trên SUT rồi điền lại |
| TC-BVA-08 | Mật khẩu mới tại biên: 8 ký tự hợp lệ | Người dùng đã qua OTP hợp lệ | Email: `test@eshop.com`;<br>OTP: hiển thị demo;<br>Mật khẩu mới: `Passw0rd@1`;<br>Xác nhận: `Passw0rd@1` | 1. Nhập OTP hiển thị demo<br>2. Nhập mật khẩu mới `Passw0rd@1`<br>3. Nhập xác nhận `Passw0rd@1`<br>4. Gửi form | Hệ thống chấp nhận mật khẩu mới nếu thỏa toàn bộ policy |  |  |  |  | Cần người dùng thực thi thủ công hoặc chạy trên SUT rồi điền lại |
| TC-BVA-09 | Mật khẩu mới trên biên: 9 ký tự hợp lệ | Người dùng đã qua OTP hợp lệ | Email: `test@eshop.com`;<br>OTP: hiển thị demo;<br>Mật khẩu mới: `Passw0rd@12`;<br>Xác nhận: `Passw0rd@12` | 1. Nhập OTP hiển thị demo<br>2. Nhập mật khẩu mới `Passw0rd@12`<br>3. Nhập xác nhận `Passw0rd@12`<br>4. Gửi form | Hệ thống chấp nhận mật khẩu mới nếu thỏa toàn bộ policy |  |  |  |  | Cần người dùng thực thi thủ công hoặc chạy trên SUT rồi điền lại |

Ghi chú trùng lặp:
- TC-BVA-08 và TC-BVA-09 trùng một phần mục tiêu với TC-01 ở Domain Testing nhưng vẫn giữ lại vì đại diện các điểm biên của mật khẩu.
- TC-BVA-02 đến TC-BVA-05 có thể trùng logic với TC-01 nếu cùng dữ liệu hợp lệ, nhưng vẫn giữ lại vì đại diện biên của OTP.
- TC-BVA-07 có thể trùng với TC-08 ở Domain Testing nhưng vẫn giữ lại vì đại diện giá trị ngay dưới biên độ dài mật khẩu.


## AI Gap Analysis
1. Thiếu bao phủ cho các điều kiện (AND) của Policy Mật khẩu
Mô tả vấn đề: Policy mật khẩu yêu cầu thỏa mãn đồng thời 4 yếu tố độc lập: (1) Chữ hoa, (2) Chữ thường, (3) Chữ số, (4) Ký tự đặc biệt hợp lệ. Theo chuẩn Equivalence Partitioning, một tập hợp các điều kiện AND đòi hỏi phải có các test case kiểm tra việc "thiếu riêng lẻ" từng điều kiện một (Negative Testing). Hiện tại, bộ test case mới chỉ cover lỗi độ dài và thiếu chung chung ký tự đặc biệt.

Rủi ro: Hệ thống có thể bị lọt bug nếu validator của Dev chỉ check 3/4 điều kiện hoặc regex bị sai (ví dụ: cho phép ký tự đặc biệt nằm ngoài whitelist).

2. Bỏ sót phân lớp "Giá trị rỗng/trống" (Blank/Empty Classes)
Mô tả vấn đề: Trong kiểm thử miền (Domain Testing), "Rỗng" (Null/Empty) là một phân lớp tương đương hoàn toàn độc lập và tách biệt với lớp "Sai định dạng" (Invalid Format) hay "Hợp lệ" (Valid). Bất kỳ form nhập liệu nào cũng phải validate trường hợp người dùng cố tình để trống hoặc chỉ nhập dấu cách (Space).

Rủi ro: Nếu bỏ qua, hệ thống có thể gặp lỗi crash (NullPointerException) hoặc hành vi UI không hiển thị đúng câu thông báo Required field (Trường bắt buộc).